import { Router } from 'express';

const router = Router();

router.get('/login', () => {});

router.get('/callback', () => {});

router.get('/logout', () => {});

router.get('/me', () => {});

export default router;
